# CTC_smart_queue
C++ 20 implement a smart queue, maintain order of incoming ticker updates only most recent price  mutex based implementation. then improve it with lock-free (memory order)    pushing quotes of stock symbols, implement queue to handle ask price of stock    need to be in relevant order. Instead of string, you can use Enum    
